# Jekyll 构建产物
_site/
.jekyll-cache/
.jekyll-metadata

# 本地依赖（bundle install --path vendor/bundle）
vendor/

# Obsidian：个人窗口布局状态不入库（每次开关都会变，避免频繁触发 CI）
.obsidian/workspace.json

# 操作系统
.DS_Store
Thumbs.db

# Node
node_modules/
